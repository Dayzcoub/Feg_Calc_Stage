# v3.1.6 — desktop wheel scroll bridge

Исправлено:
- на full-width desktop добавлен безопасный wheel/trackpad bridge для standalone shell;
- если колесо мыши попадает на canvas/grid/обёртки конструктора, страница всё равно прокручивается;
- ручное рисование/drag на canvas не менялось;
- мобильная прокрутка и расчётная логика не трогались.
