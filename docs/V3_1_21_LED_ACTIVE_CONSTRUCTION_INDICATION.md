# v3.1.21 — LED active construction indication restore

Исправлено:
- возвращена активная подсветка выбранной LED-конструкции в блоке `Active construction`;
- возвращена цветовая индикация добавленных конструкций в списке конструкций;
- добавлен компактный индикатор активной конструкции: цветовая точка, имя, количество кабинетов;
- select активной конструкции получает `data-active-color`, а кнопки списка получают `active/is-active` и `aria-pressed`.

Принцип:
- правка сделана через общий LED render/state и глобальный `styles/led.css`;
- без локальных костылей под один экран;
- расчёты, PDF, BOM, складская логика и quick catalog не изменялись.
