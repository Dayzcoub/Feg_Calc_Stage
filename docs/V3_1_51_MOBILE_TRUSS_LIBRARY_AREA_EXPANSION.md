## v3.1.51 — Mobile truss library area expansion

Что сделано:

- В standalone mobile-конструкторе ферм раскрыта область библиотеки `Прямые фермы / 2D узлы / 3D узлы`.
- Убрано жёсткое ограничение `max-height: 30svh` и внутренний scroll у `.v4-truss-library`, из-за которого секции визуально обрезались.
- Библиотека теперь растёт по содержимому и отдаёт общий scroll странице, а не режет высоту внутри себя.
- Компактная мобильная композиция библиотеки из v3.1.50 сохранена.

Что не менялось:

- Desktop layout
- Расчёты и бизнес-логика
- BOM / PDF / склад / legacy-v3 / backend writes
