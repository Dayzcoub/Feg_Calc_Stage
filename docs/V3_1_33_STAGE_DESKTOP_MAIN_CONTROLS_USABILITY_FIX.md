# v3.1.33 — Stage desktop main controls usability fix

Что исправлено:
- отменена неудачная плотная 3-колоночная компоновка основного desktop-блока сцены;
- блок переведён в читаемую 2-колоночную сетку для узкой desktop-панели;
- поля получили одинаковую высоту, padding, radius и одинаковый ритм отступов;
- `Перекладина` сделана горизонтальным field-like элементом на всю ширину;
- `Включить закрытие торцов` вынесено в ровную строку на всю ширину;
- mobile/tablet не затрагивались.

Scope:
- только `@media (min-width: 1180px)`.
