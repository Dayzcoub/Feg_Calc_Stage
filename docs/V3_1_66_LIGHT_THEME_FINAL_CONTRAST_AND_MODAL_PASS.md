## v3.1.66 — Light theme final contrast and modal pass

Что сделано:

- Светлая тема: отдельно отполирована модалка проверки нагрузок у фермы.
- Светлая тема: убраны оставшиеся тёмные поверхности внутри `dialog`/`form`/`v4-truss-load-panel`, включая вложенные поля, статусы и legacy inline backgrounds.
- Светлая тема: усилен контраст добавленных LED-модулей в конструкторе: цветовые блоки стали насыщеннее, с более читаемой рамкой и активным состоянием.
- Светлая тема: усилен контраст добавленных модулей сцены: выбранные ячейки настила стали заметнее на светлой сетке.
- Светлая тема: добавлен финальный pass по SVG/text/canvas, чтобы подписи, контуры и активные элементы не сливались с фоном.

Что не менялось:

- Dark theme остаётся дефолтом и fallback.
- Расчёты и бизнес-логика не менялись.
- BOM / PDF logic / склад / responsive / scroll / legacy-v3 / backend writes не трогались.
