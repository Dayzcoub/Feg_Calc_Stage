# v3.1.35 — Stage desktop left rail final alignment

Что исправлено:
- левая desktop-панель сцены переведена в единый вертикальный flow вместо конфликтующих grid-слоёв;
- блок `Стоимость быстрого расчёта` теперь занимает ту же ширину, что верхний control-card;
- блок `Масштаб поля` занимает ту же ширину левой панели;
- нижний ряд `По размеру / Центр / авто-fit` сделан через 12-колоночную сетку как три равных компонента по 4 колонки;
- у `auto-fit` задана такая же высота, ширина, центрирование и box-sizing, как у двух соседних кнопок.

Scope:
- только desktop `@media (min-width: 1180px)`;
- mobile/tablet не трогались.
