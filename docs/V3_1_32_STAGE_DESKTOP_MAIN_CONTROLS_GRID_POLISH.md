# v3.1.32 — Stage desktop main controls grid polish

Что исправлено:
- desktop-блок основных настроек сцены приведён к ровной 3-колоночной сетке;
- label больше не забирает ширину у select/input: подписи стоят над полями;
- все окна ввода и отображения получили единый ритм ширины/высоты, padding, border-radius и gap;
- `Перекладина` и `Включить закрытие торцов` выровнены по той же сетке;
- исправлена обрезка значений `Тип настила` и `Тип столбов`.

Scope:
- только `@media (min-width: 1180px)`;
- mobile/tablet не затрагивались.
